class Test {
    math: AdvancedMath <- new AdvancedMath;
    result: Int;

    testAdd(): Int {
        result <- math.add(5, true)  
    };
};

class AdvancedMath {
    add(x: Int, y: Int): Int {
        x + y
    };
};

class Main {
    t : Test <- (new Test);
    main() : Test {
        t
    };
};
